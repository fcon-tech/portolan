# PI Review Lane: GLM 5.1

Date: 2026-06-02
Model: `zai/glm-5.1`
Harness: `pi --no-tools --no-context-files --no-session`

## Verdict

not ready at review time:

- One critical and one major packet-audit finding had to be fixed before PR
  readiness.

## Findings

critical:

- The rule pack content or verifiable hash was not visible in the review packet.

major:

- Hash and size values were not inlined, so SC-002 was not auditable from the
  packet.

minor:

- Overscoped attempt artifact status needed a note.
- Stderr excerpt needed to be marked verbatim or summarized honestly.
- Rule pack provenance needed a note.
- Raw and derived outputs needed to be distinguished.
- The plan's baseline commands were generic but still required by repo policy.

## Disposition

accepted and fixed:

- Inlined the local rule pack YAML.
- Inlined `sha256.txt` and `sizes.txt` values.
- Split raw/primary and derived output file lists.
- Added rule pack provenance.
- Replaced the cleaned stderr block with a verbatim excerpt.
- Added a note that the overscoped attempt produced no counted evidence.

rejected:

- Removing `go test`, `go vet`, `jq`, and `git diff --check`; these are
  repository baseline checks required by AGENTS.md even for docs/evidence
  slices.
