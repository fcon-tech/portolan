# v0.1.0 Product Claim Scan

**Date**: 2026-05-30

## Commands

```bash
rg -n "Bigtop|Cursor|OpenCode|security|runtime|complete|not_assessed|failed|blocked|rejected|accepted|narrowed" docs/product-claims.md docs/releases/v0.1.0.md docs/test-corpora/apache-bigtop.md README.md -S
rg -n "Homebrew|brew install|Docker|docker pull|npm install|apt install|yum install|prebuilt" README.md docs -S
```

## Result

State: `verified`

- `docs/releases/v0.1.0.md` uses the accepted/narrowed claim frame from
  `docs/product-claims.md`: local-first evidence-preparation CLI, current
  verified surfaces, Bigtop as fixed local-scope demo/stress target, no UI
  Cursor/Composer claim, no complete estate claim, no broad security
  certification, and no adoption/popularity claim.
- Unsupported states remain visible as `not_assessed`, `failed`, `blocked`,
  `unknown`, or `cannot_verify`.
- Package-manager mentions in `docs/releases/v0.1.0.md` are negative scope
  boundaries: Homebrew, Docker, npm, apt, downloadable prebuilt binaries, and
  other package-manager routes are not part of `v0.1.0`.
- Other Docker matches are in Bigtop/configuration/research technical context,
  not public package-manager install claims.

## Not Assessed

- GitHub release publication copy is `not_assessed` until the release is
  created from `docs/releases/v0.1.0.md`.
