# v0.1.0 Release Publication Closeout

**Date**: 2026-05-31

**Release version**: `v0.1.0`

**Release commit**: `e0e23e90d4db06d134c5d1f746022145c7d52990`

**Tag**: `v0.1.0`

**GitHub release**:
`https://github.com/fcon-tech/portolan/releases/tag/v0.1.0`

## Pre-Publication Reconstruction

| Surface | State | Evidence |
| --- | --- | --- |
| Current branch | verified | `git status --short --branch` returned `## main...origin/main`. |
| Main sync | verified | `git rev-parse HEAD origin/main` returned `e0e23e90d4db06d134c5d1f746022145c7d52990` for both; `git rev-list --left-right --count HEAD...origin/main` returned `0 0`. |
| Dirty checkout | verified | `git status --short --branch` showed no file changes before tagging. |
| Open PRs | verified | `gh pr list --repo fcon-tech/portolan --state open ...` returned `[]`. |
| Release-relevant specs | verified | Backlog rows P5-047, P5-048, P5-049, and P5-050 are implemented and merged. P5-050 live Pages deployment remains outside this source-first release scope and stays `not_assessed`. |
| Existing tag | verified | `git tag --list v0.1.0` and `git ls-remote --tags origin refs/tags/v0.1.0 refs/tags/v0.1.0^{}` returned no entries before publication. |
| Existing GitHub release | verified | `gh release view v0.1.0 --repo fcon-tech/portolan ...` returned `release not found` before publication. |

## Local Release-Candidate Checks

| Check | State | Evidence |
| --- | --- | --- |
| `go test -count=1 ./...` | verified | Passed on `main` at `e0e23e90d4db06d134c5d1f746022145c7d52990`. |
| `go vet ./...` | verified | Passed with exit 0. |
| `jq empty schema/*.json internal/testfixtures/oss-adapter-contract/*.json` | verified | Passed with exit 0. |
| `git diff --check` | verified | Passed with exit 0. |
| `go run ./cmd/portolan --help` | verified | Printed expected command help and local-first/read-only note. |
| requested focused identity test | not_assessed | `go test -count=1 ./internal/app -run TestCanonicalPublicIdentityStaysAligned` matched no tests and printed `[no tests to run]`; this stale test name is not counted as verification. |
| current focused identity tests | verified | `go test -count=1 ./internal/app -run 'TestCanonicalPublicIdentity(IsPresent|OldGoImportPathIsAbsent)'` passed. |
| public identity scan | verified | `rg -n "github.com/(fcon-tech|fall-out-bug)/portolan|go install|git clone" README.md docs go.mod internal -S` found expected `github.com/fcon-tech/portolan` install, clone, module, docs, site, and internal import references. |

## Clean-Checkout Smoke

| Check | State | Evidence |
| --- | --- | --- |
| Clone | verified | `git clone https://github.com/fcon-tech/portolan.git /tmp/portolan-release-smoke.BhZ7iI` succeeded. |
| Clean clone commit | verified | `git rev-parse HEAD` in the clone returned `e0e23e90d4db06d134c5d1f746022145c7d52990`. |
| Bootstrap | verified | `scripts/bootstrap-portolan` wrote `/tmp/portolan-release-smoke.BhZ7iI/.portolan/bin/portolan`. |
| Version smoke | verified with caveat | `.portolan/bin/portolan --version` printed `portolan dev`, which is expected for source-checkout bootstrap without release ldflags. |
| Context prepare smoke | verified | `.portolan/bin/portolan context prepare --root . --out /tmp/portolan-context-smoke --profile cursor --force` wrote `/tmp/portolan-context-smoke`. |
| Map smoke | verified | `.portolan/bin/portolan map --root . --out /tmp/portolan-map-smoke --force` wrote `/tmp/portolan-map-smoke`. |

## GitHub Checks And Alerts

| Surface | State | Evidence |
| --- | --- | --- |
| GitHub Actions for release commit | verified | `gh run list --repo fcon-tech/portolan --branch main --commit e0e23e90d4db06d134c5d1f746022145c7d52990 ...` returned successful completed runs: `CI`, `Code Quality: Push on main`, and `Push on main`. |
| Code scanning open alerts | verified | `gh api 'repos/fcon-tech/portolan/code-scanning/alerts?state=open&per_page=100' --paginate | jq 'length'` returned `0`. |
| Dependabot open alerts | verified | `gh api 'repos/fcon-tech/portolan/dependabot/alerts?state=open&per_page=100' --paginate | jq 'length'` returned `0`. |

## Publication

| Step | State | Evidence |
| --- | --- | --- |
| Annotated tag | verified | `git tag -a v0.1.0 -m "Portolan v0.1.0" e0e23e90d4db06d134c5d1f746022145c7d52990` succeeded. |
| Tag push | verified | `git push origin v0.1.0` created remote tag `v0.1.0`. |
| Remote tag target | verified | `git ls-remote --tags origin refs/tags/v0.1.0 refs/tags/v0.1.0^{}` returned tag object `0d2b78470cff633fbfcf6cbf97e82a04f43d4916` and peeled commit `e0e23e90d4db06d134c5d1f746022145c7d52990`. |
| GitHub release | verified | `gh release create v0.1.0 --repo fcon-tech/portolan --title "Portolan v0.1.0" --notes-file docs/releases/v0.1.0.md --target e0e23e90d4db06d134c5d1f746022145c7d52990` returned `https://github.com/fcon-tech/portolan/releases/tag/v0.1.0`. |
| Release state | verified | `gh release view v0.1.0 ...` returned `isDraft:false`, `isPrerelease:false`, `publishedAt:2026-05-31T07:07:35Z`, and target commit `e0e23e90d4db06d134c5d1f746022145c7d52990`. |

## External Install Smoke

| Check | State | Evidence |
| --- | --- | --- |
| `go install ...@v0.1.0` | verified with caveat | `GOBIN=/tmp/portolan-install-v010/bin GOMODCACHE=/tmp/portolan-install-v010/pkg/mod go install github.com/fcon-tech/portolan/cmd/portolan@v0.1.0` resolved the public tag and built the binary. |
| Installed binary version | verified with caveat | `/tmp/portolan-install-v010/bin/portolan --version` printed `portolan dev`. The versioned source install path works, but Go install does not inject `internal/app.Version=v0.1.0` without release ldflags. |

## Product-Claim Scan

| Surface | State | Evidence |
| --- | --- | --- |
| Release note claim boundary | verified | `docs/releases/v0.1.0.md` stays within `docs/product-claims.md`: source-first release, no UI Cursor/Composer claim, no complete inherited-estate claim, complete runtime topology `not_assessed`, no broad security certification, and no adoption/popularity claim. |
| Package-manager scope | verified | Release notes explicitly exclude downloadable prebuilt binaries, Homebrew, Docker, npm, apt, and other package-manager routes from `v0.1.0`. |
| Badge scope | verified | OpenSSF Best Practices and Scorecard badges remain outside this release; `docs/specs/048-github-community-discovery/reviews/openssf-best-practices-self-assessment-2026-05-31.md` keeps Best Practices as prepared for maintainer submission, not achieved. |

## Blocked Or Not Assessed Surfaces

| Surface | State | Evidence |
| --- | --- | --- |
| Prebuilt binaries | not_assessed | Out of scope for source-first `v0.1.0`. |
| Homebrew, Docker, npm, apt, other package-manager routes | not_assessed | Out of scope for source-first `v0.1.0`. |
| GitHub Pages deployment, default Pages URL, DNS, HTTPS | not_assessed | P5-050 records these as outside the merged site proof; no Pages claim is part of this release. |
| OpenSSF Best Practices badge | not_assessed | Requires authenticated Best Practices project creation and visible badge state. |
| OpenSSF Scorecard badge | not_assessed | No Scorecard badge is claimed. |
| Adoption/popularity/customer/production usage | not_assessed | No adoption, popularity, star, fork, customer, or production-usage claim is made. |
| Human GitHub review approval | not_assessed | No review approval is required for the already merged release commit; not used as release evidence. |

## Decision

`v0.1.0` is published as the first source-first Portolan release from commit
`e0e23e90d4db06d134c5d1f746022145c7d52990`.

The public source install path is verified, with the explicit version-output
caveat that `go install ...@v0.1.0` prints `portolan dev` because release
ldflags are not injected by the Go install route.
