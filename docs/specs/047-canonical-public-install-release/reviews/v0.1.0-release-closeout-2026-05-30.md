# v0.1.0 Release Closeout

**Date**: 2026-05-30

**Release version**: `v0.1.0`

**Branch**: `codex/047-canonical-public-install-release`

## Local Verification

| Check | State | Evidence |
| --- | --- | --- |
| `go test -count=1 ./...` | verified | Passed for all packages under module `github.com/fcon-tech/portolan`. |
| `jq empty .specify/feature.json schema/*.json` | verified | Passed with exit 0. |
| `git diff --check` | verified | Passed with exit 0. |
| canonical identity regression | verified | `TestCanonicalPublicIdentityIsPresent`, `TestCanonicalPublicIdentityOldGoImportPathIsAbsent`, and `TestReleaseBuildCanInjectVersion` passed in focused and full test runs. |
| stale Go import scan | verified | `rg -n "github.com/fall-out-bug/portolan" --glob '*.go' .` returned no matches. |
| source-checkout smoke | verified | `git clone https://github.com/fcon-tech/portolan.git /tmp/portolan-source-smoke && scripts/bootstrap-portolan && .portolan/bin/portolan --version` produced `portolan dev` from current public main. |
| versioned public `go install` smoke | blocked | `go install github.com/fcon-tech/portolan/cmd/portolan@v0.1.0` failed because `v0.1.0` is not tagged/published yet and current public main still has the old module path. |
| versioned build/checksum smoke | verified | Local build with `-X github.com/fcon-tech/portolan/internal/app.Version=v0.1.0` produced `portolan v0.1.0` and checksum `a1ae267f6a11cf06deccba7c82786ae46b5e87906a8b106b744cc780194301df`. |
| product-claim scan | verified | Release notes stay within `docs/product-claims.md` boundaries and preserve unsupported states. |
| package-manager claim scan | verified | Homebrew/Docker/npm/apt mentions in release notes are negative scope boundaries; other Docker matches are technical Bigtop/configuration context, not install claims. |

## Review Evidence

| Review | State | Artifact |
| --- | --- | --- |
| plan/task review | verified | `plan-task-pi-review-disposition-2026-05-30.md` |
| implementation review | verified | `implementation-review-disposition-2026-05-30.md` |
| failed/degraded lanes | verified | Direct `minimax/MiniMax-M2.7` returned 404 and is recorded as `not_assessed`; OpenRouter MiniMax replacement was assessed. |

## External State

| Surface | State | Evidence |
| --- | --- | --- |
| GitHub checks | verified | PR #22 `Baseline` check passed. |
| GitHub release publication | not_assessed | `v0.1.0` release has not been published. |
| Adoption/popularity | not_assessed | No adoption, star, fork, production, or customer claim is made. |
| Merge approval | verified | User approved merge in chat on 2026-05-30 with "Согласовано слияние". |

## Stop Reason

PR #22 is ready-for-review with GitHub CI verified. The versioned public
`go install` route remains blocked until this branch is merged and `v0.1.0` is
tagged/published.

## Review Artifact Retention

The review files under `docs/specs/047-canonical-public-install-release/reviews/`
are permanent SpecKit evidence for this trust-sensitive release slice. They are
not product docs and should not be treated as public install instructions.
Future release slices may replace the template with a generated checklist only
after the first public release process is proven.
