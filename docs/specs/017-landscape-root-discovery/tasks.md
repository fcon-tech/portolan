# Tasks: Landscape Root Discovery

- [x] T001 Add tests for `map --root` over direct child repos and `repos/*`
  repos without a selection file.
- [x] T002 Implement bounded root repository discovery and generated in-memory
  selection targets.
- [x] T003 Add coverage records for skipped/non-Git candidates and external
  completeness `unknown`.
- [x] T004 Preserve existing single-root and `map --selection` behavior.
- [x] T005 Update blind acceptance and Cursor/agent docs away from
  `selection.json` as first-run input.
- [x] T006 Run baseline checks, fixture smoke, and record implementation
  disposition.
