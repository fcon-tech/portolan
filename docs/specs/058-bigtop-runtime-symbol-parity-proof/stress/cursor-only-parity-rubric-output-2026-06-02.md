Assessment uses only non-Portolan workspace artifacts: `selection.json`, `repos/**`, and `.cursor/**`.

| criterion | answer | evidence_used | status | remaining_gap |
| --- | --- | --- | --- | --- |
| **C1 — Landscape scope and role map** | Declared scope is 15 repositories in `selection.json` (alluxio, airflow, bigtop-repo, flink, hadoop, hbase, hive, kafka, phoenix, ranger, solr, spark, tez, zeppelin, zookeeper). On disk there are 18 top-level repos; `apache-livy`, `apache-oozie`, and `apache-sqoop` exist under `repos/` but are not in the selection. `apache-bigtop-repo` acts as packaging/deploy/test hub; upstream repos are component sources. Bigtop deploy role map maps components to cluster roles (namenode, resourcemanager, hive-server2, kafka-server, etc.) in Puppet. | `selection.json`; repo root markers (`repos/*/pom.xml`, `repos/*/build.gradle`); `repos/apache-bigtop-repo/README.md`; `repos/apache-bigtop-repo/bigtop-deploy/puppet/manifests/cluster.pp` | **partial** | No workspace-local landscape manifest or unified role map across all 18 repos; `corpus_manifest` points to a missing external file; selection vs checkout mismatch (15 vs 18). |
| **C2 — Static dependency and relationship graph** | Dependencies are embedded in source/build files (Maven `pom.xml`, Gradle, RPM specs, Puppet `$roles_map`) but there is no precomputed landscape-wide dependency or relationship graph in the workspace outside Portolan. Cursor can infer local edges (e.g., Hive → Hadoop in `repos/apache-hive/**/pom.xml`) via search, not a complete cross-repo graph. | `repos/apache-hive/streaming/pom.xml` (Hadoop deps); `repos/apache-bigtop-repo/pom.xml` (component version properties); `repos/apache-bigtop-repo/bigtop-packages/src/rpm/*/SPECS/*.spec`; `cluster.pp` `$roles_map` | **partial** | No machine-readable graph artifact (SBOM, graph.json, depends-on index) at landscape root; cross-repo edges require ad-hoc synthesis and will be incomplete. |
| **C3 — Deployment model** | Bigtop deployment is documented as Docker provisioning (`provisioner/docker/docker-compose.yml`), Puppet cluster orchestration (`bigtop-deploy/puppet/`), RPM/DEB packaging (`bigtop-packages/`), plus per-component Docker/K8s assets (e.g., Alluxio K8s/Helm under `repos/alluxio/integration/kubernetes/`). | `repos/apache-bigtop-repo/README.md`; `repos/apache-bigtop-repo/provisioner/docker/docker-compose.yml`; `repos/apache-bigtop-repo/bigtop-deploy/puppet/manifests/cluster.pp`; `repos/apache-bigtop-repo/bigtop-packages/src/rpm/` and `deb/` trees; `repos/alluxio/integration/kubernetes/` | **verified** | Models are static/config-as-code only; no single consolidated deploy topology doc tying all 15 selected repos into one runnable stack definition in the workspace root. |
| **C4 — Runtime topology** | `selection.json` sets `"runtime": null`. No non-Portolan runtime observations (running containers, processes, live endpoints, orchestrator state) were found in the workspace. Puppet/Docker files describe intended layout, not observed live topology. | `selection.json` (`runtime: null`); static only: `docker-compose.yml`, `cluster.pp` | **cannot_verify** | Live cluster state, service endpoints, and orchestrator inventory are absent; Cursor cannot prove what is actually running. |
| **C5 — API/catalog/model surfaces** | Component catalog surfaces exist via Bigtop packaging specs (~19 packaged components: hadoop, hive, hbase, kafka, spark, etc.). API/model surfaces exist in-repo (e.g., 27 Alluxio `.proto` files; Spark Connect protobuf README). No unified landscape-level API/catalog index. | `repos/apache-bigtop-repo/bigtop-packages/src/rpm/*/SPECS/*.spec`; `repos/alluxio/core/transport/src/main/proto/`; `repos/apache-spark/sql/connect/common/src/main/protobuf/README.md` | **partial** | Surfaces are scattered per repo; no consolidated catalog linking APIs, schemas, and packaged components across the full selection. |
| **C6 — Symbol/reference graph** | No producer output with symbol definitions plus references for a declared selected scope exists outside Portolan. Cursor can do point-in-time grep/read on source, but that is not a persisted, scope-complete symbol/reference graph. | Absence check: no `gopls-*`, `graph.json`, or symbol TSV at workspace root; only `.portolan/**` contains such artifacts (excluded) | **cannot_verify** | Full def/ref graph for selected scope would require running and storing language-server or indexer output; none present in Cursor-only workspace artifacts. |
| **C7 — Evidence-state discipline** | `selection.json` has schema fields (`schema_version`, `targets`, null placeholders for `runtime`, `claims`, `tool_outputs`) but no populated evidence index, gap ledger, or answer contract at landscape root. Formal evidence-state machinery lives only under `.portolan/` (excluded). | `selection.json`; absence of root-level `evidence-index.jsonl`, `gaps.jsonl`, `answer-contract.md` | **partial** | Cursor can cite files in prose, but there is no machine-enforced evidence contract, gap tracking, or claim/evidence linkage for Bigtop in this workspace. |
| **C8 — Cursor augmentation value** | Cursor adds interactive code search/read and can navigate multi-repo source directly. Workspace-level Cursor config is minimal (`.cursor/hooks/state/continual-learning.json` only). Some upstream repos ship `AGENTS.md` (notably Airflow, Spark, Solr, HBase, Flink, Zeppelin), but there is no landscape-specific Cursor rule/skill/index layer. | `.cursor/hooks/state/continual-learning.json`; `repos/apache-airflow/AGENTS.md` and other per-repo `AGENTS.md` files; `selection.json` as scope hint | **partial** | No prebuilt navigation index, producer pipeline, or landscape rules; augmentation is opportunistic search, not systematic code intelligence. |
| **C9 — Enterprise parity threshold** | Enterprise code-intelligence parity would require verified scope/role map, complete static graph, runtime topology, full symbol graph, and evidence discipline. Cursor-only artifacts satisfy deployment-model knowledge and partial scope/catalog discovery, but fail on runtime, symbol graph, unified graph, and formal evidence gates. | Synthesis of C1–C8 findings above | **cannot_verify** | Missing: live runtime proof, persisted cross-repo dependency/symbol graphs, evidence-index discipline, and alignment between selection (15) and checkout (18). |

**Can Cursor alone prove enterprise code-intelligence parity for Bigtop here?** No — the workspace gives Cursor strong read/search access to a multi-repo Hadoop ecosystem checkout and static deploy/packaging knowledge, but it lacks persisted cross-repo graphs, runtime observations, producer-grade symbol/reference output, and formal evidence-state artifacts needed to prove enterprise parity.
