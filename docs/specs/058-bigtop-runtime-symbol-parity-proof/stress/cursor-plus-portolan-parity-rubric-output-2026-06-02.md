## Cursor Plus Portolan Parity Rubric (Bigtop landscape)

Selected scope: 18 discovered repos under `repos/`, with bounded producer outputs from stress runs **054** (initial proof), **057** (producer expansion), and **058** (runtime/symbol parity probe). Statuses follow the Portolan evidence summary: nothing is upgraded beyond what producers actually supply.

| criterion | answer | portolan_evidence_used | status | remaining_gap | delta_vs_cursor_only_expected |
| --- | --- | --- | --- | --- | --- |
| **C1** Landscape scope and role map | 18 local repos are inventoried with `source-visible` paths; Bigtop integration roles are inferable only from static files (e.g. `bigtop.bom`, Puppet), not as a verified enterprise role map. | `repos.json`, `coverage.json` (054), `map/summary.json` (18 repos; `external-completeness` **unknown**); baseline stress compares file-backed Bigtop BOM/Puppet links vs Portolan inventory | **partial** | No curated manifest; per-repo service roles mostly **not_assessed**; ecosystem completeness **unknown** | Cursor-only can narrate roles from ad hoc reads but often blurs “packaging order” vs runtime; Portolan adds explicit repo IDs, coverage records, and `gap-external-completeness` |
| **C2** Static dependency and relationship graph | Queryable graph exists (~172k nodes / ~149k edges in 054 map), but semantic coupling is thin: native Go `imports`/`depends-on` for **alluxio** + **apache-airflow** only; ~147k `unknown` nodes. | Portolan **map** (`graph.json`, `graph-index.json`, `findings.jsonl`); optional **Syft/CycloneDX** in stress `145759` (not merged into 054 `gap-cyclonedx-not-assessed`); **jscpd** bounded to `apache-bigtop-repo/bigtop-test-framework` (057) | **partial** | JVM/Scala/PHP/Maven import graphs **not_assessed**; SBOM not selected in 054 context; graph size ≠ architecture coverage per `summary.json` warnings | Cursor-only may cite POM snippets convincingly but without graph/query discipline; Portolan supplies bounded, queryable slices and flags non-Go as **not_assessed** |
| **C3** Deployment model | Static deploy artifacts are evidenced for Bigtop Docker Compose and multiple Alluxio Helm renders; all are **metadata-visible**, not runtime. | **Docker Compose**: `producer-run-bigtop-compose-20260601` → `apache-bigtop-compose.config.json`; **Helm**: 054 monitor chart + 057 four `verified` templates (one `alluxio-job` **blocked**); `helm-template-status.tsv` | **partial** | Bigtop K8s/Puppet breadth not fully rendered; one Helm chart failed template; no proof deploy matches production | Cursor-only often treats Compose/Helm as “what runs”; Portolan ties outputs to `metadata-visible` + explicit “not runtime topology” limitations |
| **C4** Runtime topology | No runtime-visible observation export is selected for Bigtop; local `docker ps` shows unrelated workloads, not a Bigtop stack observation. | `producer-run-bigtop-runtime-not-assessed-20260601`; `gap-runtime-observation-not-assessed`; 058 `selection-runtime-tool-outputs.tsv` (`repo_count=0`); 058 `docker-ps.tsv` (non-Bigtop containers) | **not_assessed** | Need **runtime probe** export scoped to Bigtop (process/service/container/orchestrator) with `runtime-visible` state | Cursor-only may guess running services; Portolan blocks that claim unless a runtime producer is supplied |
| **C5** API/catalog/model surfaces | Bounded **protobuf descriptor** evidence for Alluxio (2 protos in 054; 27 proto files / expanded descriptor in 057); no landscape-wide OpenAPI/Backstage/catalog. | **protobuf descriptor**: `alluxio-grpc.descriptor.pb` (054), `alluxio-all-protos.descriptor.pb` + `alluxio-proto-files.txt` (057); gaps: `gap-openapi-not-assessed`, `gap-backstage-not-assessed`, `producer-coverage-*-api-catalog` for 17/18 repos | **partial** | Hadoop/Hive/Spark/Kafka APIs **not_assessed**; descriptors do not prove live RPC behavior | Cursor-only may over-generalize from one `.proto`; Portolan scopes to compiled descriptor files and marks other repos **not_assessed** |
| **C6** Symbol/reference graph | Selected-file **symbol listing** only (gopls on 5 Airflow Go SDK files); no definition+reference producer (SCIP/LSIF/etc.) for declared scope. | **gopls**: `gopls-airflow-go-sdk-selected-symbols.txt`, `gopls-airflow-go-sdk-selected-status.tsv` (057); `producer-run-bigtop-symbol-index-not-assessed-20260601`; 058 `symbol-runtime-tool-availability.tsv` (SCIP/ctags/lsif **NOT_FOUND**) | **partial** | No cross-repo call/reference graph; gopls output is symbols per file, not a queryable def/ref index for Bigtop | Cursor-only can list symbols in open files but lacks bounded producer proof; Portolan labels full symbol-index **not_assessed** |
| **C7** Evidence-state discipline | Contract and gap registry are strong; central `producer-runs.jsonl` still reflects 054 (runtime/symbol placeholders) while 057/058 outputs live mainly under stress dirs. | `answer-contract.md`, `gaps.jsonl`, `evidence-index.jsonl` (054); `producer-runs.jsonl`; stress comparison outputs (`145759` Portolan vs clean baseline) | **partial** | Not every 057/058 artifact is ingested as `producer-run` records with run IDs in the canonical index; agents must cite stress paths explicitly | Cursor-only answers are less systematically bounded; Portolan+contract reduces false certainty and names **unknown** / **not_assessed** surfaces |
| **C8** Cursor augmentation value | Same questions become more defensible on scope, static deploy, and bounded API slices; runtime and enterprise graph questions stay honestly open. | Side-by-side stress: `cursor-composer25-no-portolan-clean-baseline-output.md` vs `cursor-composer25-output.md` / `054` producer-runs assessment | **partial** | Improvement is discipline and gap attribution, not completeness; large node counts must not be misread as coverage | Expected: fewer over-claims on runtime/topology/API breadth; clearer “narrowed” verdicts vs confident architecture narratives |
| **C9** Enterprise parity threshold | Fails threshold: C4 and full C6 are **not_assessed** / partial; C1–C3, C5, C7–C8 are partial at best. | Aggregate of C1–C8; enterprise code-intelligence parity explicitly **not_assessed** in supplied summary | **not_assessed** | Would require verified C1–C8 for selected scope including runtime topology and symbol/reference graph (or a formally narrowed claim excluding them) | Cursor-only cannot reach enterprise parity either; Portolan mainly prevents pretending parity exists |

**Can Cursor plus Portolan prove enterprise code-intelligence parity for Bigtop here?** No — they support bounded, evidence-disciplined Q&A (inventory, static deploy manifests, Alluxio proto descriptors, selected Go symbols, partial static graphs), but runtime topology and a full symbol/reference graph remain **not_assessed**, so enterprise parity cannot be proven for the declared Bigtop scope.
