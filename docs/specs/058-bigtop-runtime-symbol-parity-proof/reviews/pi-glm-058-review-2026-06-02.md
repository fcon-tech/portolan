# Spec 058 Review Findings

| # | Severity | Evidence | Recommendation | Verdict | Not Assessed |
|---|----------|----------|----------------|---------|-------------|
| 1 | **low** | Spec restates the user's original Russian goal ("доводя до verified…") accurately as an *objective*, then immediately bounds it through FR-003/FR-004/FR-005/FR-006 which forbid overclaim. The slice does not redefine "understand architecture like a human" into something weaker; it constructs a rubric (C1–C9) that operationalizes what "verified" would mean and then honestly reports which criteria remain `not_assessed`. | No change. The spec-to-goal trace is honest and explicitly deflationary where evidence is absent. | **Honest** | — |
| 2 | **none** | Runtime topology probes (T006–T008) check selection metadata, local Docker containers, and existing `.portolan` outputs. All return null/unrelated. Spec records `not_assessed` and a precise "next evidence needed" statement. FR-004 explicitly forbids promoting static Compose/Helm to runtime. | No change. Blocker is genuine: no safe local runtime export exists. | **Honest** | Runtime topology |
| 3 | **none** | Symbol/reference probes (T009–T011) check for `scip`, `ctags`, `universal-ctags`, `lsif-java`, `lsif-go`, `src-cli`; none installed. Partial tools (`gopls`, `javap`) exist but cannot produce full def/ref graph. Spec correctly records `not_assessed`. FR-005 prevents file-symbol listings from being promoted. | No change. Blocker is genuine: no full graph producer available locally. | **Honest** | Full symbol/reference graph |
| 4 | **medium** | Cursor stress (T012–T015) compares two lanes against the same C1–C9 rubric. However, the rubric itself was designed *within this slice* to measure "human or enterprise code-intelligence parity" — a concept originating from the same user goal. This is not circular per se (the rubric is a measurement contract, not a claim), but the stress proves only: (a) Cursor+Portolan has better evidence discipline and gap attribution than Cursor alone, and (b) neither lane achieves runtime, full symbol, or enterprise parity. It does **not** prove anything about "architecture understanding" beyond the bounded evidence surfaces already captured in specs 054–057. The rubric's C9 correctly blocks enterprise parity when C4/C6 are missing, which is the right restraint. | Clarify in the accepted result that Cursor stress validates *rubric scoring discipline*, not architecture coverage. Current wording is already close; add one sentence to the stress ledger or plan stating: "Stress scope is bounded to rubric scoring consistency and gap attribution, not to architecture-understanding breadth." | **Acceptable but scope-adjacent** | Unbounded architecture understanding beyond evidence surfaces |
| 5 | **none** | Tasks phase structure is coherent: setup → runtime probe → symbol probe → parity rubric/stress → final. All probe tasks are checked complete; T017/T018 remain open. The ledger, rubric, and stress scoring are consistent with each other and with the constitution's evidence-state rules. | No change. Task surface is coherent. | **Coherent** | — |
| 6 | **low** | The parity rubric pre-stress assessment table assigns C7 as "verified scoped." This is fair for specs 054–057 evidence-state discipline within bounded packets, but the rubric's own C7 definition requires "every claim carries verified/partial/not_assessed/cannot_verify boundary and cites producer/run IDs." The 058 stress outputs are not yet canonical producer-run records (C7 scoring row itself notes this). Calling C7 "verified scoped" is a minor overclaim within the rubric's own terms. | Downgrade C7 pre-stress assessment to "partial" or add a footnote that "verified scoped" applies only to 054–057 packets, not to 058 stress artifacts which are not yet canonical. | **Minor overclaim on C7 self-assessment** | — |
| 7 | **none** | Evidence correction note at bottom of stress ledger documents a schema key mismatch (`.repositories` vs `.targets`). The correction is transparent and does not change the conclusion. | No change. Good practice. | **Honest** | — |

## Summary Verdict

Spec 058 does **not** wrongly redefine the user goal. It translates an ambitious objective into a deflationary measurement framework and honestly reports three major criteria as `not_assessed`. Runtime and symbol blockers are genuine — no safe local producers exist. Cursor+Portolan stress proves **rubric scoring discipline and gap attribution improvement only**, which is appropriately narrow. Tasks and status surfaces are coherent. One minor finding: C7 self-assessment as "verified scoped" is slightly generous given the rubric's own producer-run citation requirement.
