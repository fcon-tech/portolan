# Spec 071 Review: Bigtop Ctags Cross-Language Imports

## findings:

- **severity: minor**
  evidence: FR-005 / producer result — 347,610 total records with 147,472 reference-role records and claim "bounded source-visible cross-language reference-role evidence, not full C6"
  issue: Claim is appropriately bounded and self-limiting. The breakdown by role (imported, namespace, indirectlyImported, system, local, loaded) is internally consistent and aligns with what `--extras=+r` produces. No overclaim detected. Finding is minor only to note that "reference-role" includes `namespace` (51,707) which are structural/definition roles rather than import-reference roles in the strictest sense — but ctags classifies them under `--extras=+r` so the provenance is correct.
  recommendation: No action required. The spec already disclaims full C6 (FR-005, FR-006). Optionally add a one-line note in the ledger that `namespace` roles are definition-scoped not import-scoped for future readers.

- **severity: minor**
  evidence: FR-003 / producer result — Python unknown imported 74,674 constitutes ~50.6% of all reference-role records
  issue: The kind `unknown` for Python imported references suggests ctags could not resolve the target symbol type. This is expected behavior for Universal Ctags on Python imports (it sees `import X` but cannot resolve what `X` is without runtime analysis), and the spec explicitly does not claim type resolution (FR-006). However, the concentration of "unknown" should be surfaced as a known gap rather than left implicit.
  recommendation: Add a brief note in the ledger or plan.md that Python imported references are predominantly kind=unknown, confirming FR-006's disclaimer is grounded in real data.

- **severity: minor**
  evidence: FR-004 / producer result — hashes, sizes recorded per requirement
  issue: No finding against this; the ledger appears complete (command, exit code, version, role breakdown, counts all present). Confirmed as satisficed.
  recommendation: None.

- **severity: minor**
  evidence: FR-007 / producer result — exit code 0, no mutation indicated
  issue: No violation detected. Command uses `-f` to write outside target paths. No build, no network tooling, no Kubernetes. Satisficed.
  recommendation: None.

- **severity: minor**
  evidence: FR-008 / Cursor stress ledger — "C6 still partial, not full symbol/reference graph" and cannot_verify items
  issue: Stress review correctly preserves the C6 boundary and does not escalate beyond what the ctags evidence supports. The honest cannot_verify classification for method/class/type refs, xref linkage, call graph, runtime topology, and enterprise parity is appropriate and aligns with FR-006.
  recommendation: None. This is correct boundary hygiene.

- **severity: minor**
  evidence: product-backlog.md P6-071 entry
  issue: Cannot verify backlog entry text from this packet alone, but no overclaim signal based on spec/plan/tasks consistency.
  recommendation: Confirm P6-071 wording matches FR-005/FR-006 bounds on merge.

## not_assessed:
- Actual content of bigtop-selected-cross-language-imports.jsonl (no tool access to verify record-level schema compliance)
- selected-target-paths.txt scope provenance (spec says reuse from 059; lineage not independently verified)
- AGENTS.md and feature.json syntactic validity beyond spec pointer consistency
- Whether P6-071 backlog item text precisely mirrors the FR-005/FR-006 boundary language
- Cursor stress prompt/output fidelity (ledger summary reviewed, raw prompt/output not in packet)

## verdict: ready_with_minor_fixes

The spec is well-scoped, self-limiting, and honest about evidence boundaries. The producer result is internally consistent (347,610 total → 147,472 ref-role → role/language breakdown sums check out). FR-006 disclaimers are grounded in real data (Python unknown=74,674 confirms type resolution is absent). FR-007 no-mutation constraint is met. Cursor stress preserves C6 boundary correctly.

Two minor documentation improvements recommended (namespace role note, Python unknown concentration note) — neither blocks merge. No critical or major findings. No overclaims detected. No product-boundary violations.
