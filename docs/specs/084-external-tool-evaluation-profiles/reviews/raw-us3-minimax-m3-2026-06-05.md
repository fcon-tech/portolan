# US3 Review: External Tool Evaluation Profiles — spec 084

**Scope**: T016-T018 (refresh procedure, role/producer-family mapping boundary, schema non-change, maintainability).
**Verdict**: **CHANGES_REQUESTED**.

## Findings

| # | Severity | Evidence | Recommendation |
|---|----------|----------|----------------|
| F-1 | blocker | `docs/specs/084-external-tool-evaluation-profiles/reviews/us3-review-disposition-2026-06-05.md` does not exist; the only US3 raw lane file `raw-us3-minimax-m3-2026-06-05.md` is 0 bytes. T018 task is unchecked. | Create the disposition file with three assessed non-GPT lanes (or record explicit `not_assessed` per lane with replacement rationale) before claiming US3 is reviewed. Per `AGENTS.md` and the planning-review-disposition precedent, an empty/hung/minimax self-observed lane does not count as assessed evidence. |
| F-2 | high | `tasks.md:40-42` shows T016, T017, T018 as `[ ]`, but T016's deliverable (Refresh Procedure at `docs/adapter-contracts/external-tool-evaluation-profiles.md:119-127`) is present in the working tree and T017 is verifiable (`jq empty` passes for all 7 schema files; no schema file appears in the diff set). | Reconcile the ledger: mark T016 done only after the F-1 disposition is in place, record the T017 command output verbatim, then mark complete. AGENTS.md requires backlog/spec/tasks/disposition/implementation consistency. |
| F-3 | high | The US3 independent test in `spec.md:78-80` says a profile can transition `producer_candidate` -> `blocked` / `rejected` / `ready_for_import_planning` "without changing graph schemas." The Refresh Procedure step 4 (`docs/adapter-contracts/external-tool-evaluation-profiles.md:122-126`) only describes the `guidance -> import planning` direction and only says "map the decision to producer-family `Decision` and `SupportState` records." The other three transitions (to `blocked`, `rejected`, and from `producer_candidate` to `ux_pattern_source`) have no documented producer-family destination. | Make the role -> `Decision`/`SupportState` mapping explicit for all five role states (at minimum: source state, target state, required producer-family record change, whether the importer/evaluation slice is opened or closed). Either expand the Refresh Procedure or add a small mapping table to `contracts/external-tool-profile.md`. |
| F-4 | medium | `docs/product-backlog.md:168` says P6-084 is "Ready for implementation," but `spec.md:7-8` still says "Ready for implementation; plan.md and tasks.md exist; planning review is in progress before implementation," and T016-T018 are unchecked. Three SpecKit surfaces disagree on the same spec. | Pick one state (either "ready-for-implementation, US3 review in progress" or "implemented, PR review pending") and align backlog, spec status, and task ledger to it. |
| F-5 | medium | Maintainability: the Refresh Procedure is 5 lines, which is good, but it has no cross-reference to the producer-family schema or to `contracts/external-tool-profile.md`. A maintainer refreshing a profile is told to "map to producer-family Decision and SupportState records" without pointing at the field set, schema path, or an importer-slice template. | Add a single sentence pointing at `schema/producer-family.schema.json` and the slice template under `docs/specs/053-language-agnostic-producers/` (or whichever spec owns the importer opening). |
| F-6 | low | `docs/adapter-contracts/external-tool-evaluation-profiles.md:33` requires a manual one-day refresh check on `Last refreshed`. The contract has no machine-checkable freshness signal beyond the date string. | Optionally add a focused test that asserts a `last_refreshed` is present in every profile and a CLI/lint command that warns when any profile is older than 1 day. Not blocking for US3. |
| F-7 | info | F-001 from the US2 disposition (`raw-us2-zai-glm51-2026-06-05.md:25-28`) — stale-profile risk not surfaced in the generated `answer-contract.md` — also bears on US3's "incremental refresh" story. The bounded `contextprep.go` paragraph added for US2 (`contextprep.go:2434`) does not mention profile staleness. | Consider closing the US2 finding inside the US3 cycle, since refresh is the US3 theme. Low priority relative to F-1/F-2/F-3. |

## Not Assessed

- **End-to-end role transition test**: a profile has not actually been moved between role states in this slice; the independent test in `spec.md:78-80` is asserted by documentation, not by exercised behavior.
- **Producer-family schema stability across future importer slices (085/086)**: depends on specs that are explicitly backlog-only.
- **GitHub checks, PR state, CI, and merge readiness**: no PR exists yet.
- **CRAP, MI, CleanArch-hex lenses for US3**: US3 is docs-only (Refresh Procedure text). Per `AGENTS.md` these lenses are `not_applicable` for docs-only files; the bounded `contextprep.go` change is a single non-executable string literal and was already covered by the US2 code lens. YAGNI / DRY / CleanCode / SOLID: the new paragraph does not introduce a parallel decision scheme and does not duplicate the producer-family record contract, so no defect under those lenses.

## Bottom Line

US3's deliverable text (`docs/adapter-contracts/external-tool-evaluation-profiles.md:119-127`) is present and the schema non-change claim is verifiable, but the review disposition required by T018 and the project governance is missing, the task ledger is stale, and the role/producer-family mapping is not made explicit for all four non-import transitions. Resolve F-1, F-2, and F-3 before marking US3 ready for PR.
