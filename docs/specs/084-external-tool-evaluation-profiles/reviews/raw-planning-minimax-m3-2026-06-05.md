# Spec 084 Plan/Tasks Readiness Review

**Reviewer**: repo-grounded local review (no edits)
**Date**: 2026-06-05
**Scope**: `spec.md`, `plan.md`, `tasks.md`, `research.md`, `data-model.md`, `quickstart.md`, `contracts/external-tool-profile.md`, `reviews/`, `AGENTS.md` diff, backlog row P6-084, existing `internal/contextprep`, `internal/producerfamily`, `docs/adapter-contracts/*`, `docs/product-boundary.md`, `docs/product-claims.md`, `docs/review-harness-benchmark.md`.

## Findings

| # | Severity | Area | Finding | Evidence | Recommendation | Disposition |
|---|---|---|---|---|---|---|
| F1 | **Major** | SpecKit pipeline drift | T005 requires three assessed non-GPT planning lanes. Only two produced real review output: `raw-planning-kimi-k2p6-2026-06-05.md` (PASS, 4 minor findings) and `raw-planning-zai-glm51-2026-06-05.md` (PASS, 10 minor findings). The `raw-planning-minimax-m3-2026-06-05.md` lane is an **empty 0-byte file** — that is harness failure / `not_assessed` per AGENTS.md and `docs/review-harness-benchmark.md:130-138`. Coverage is 2/3. | `docs/specs/084-external-tool-evaluation-profiles/reviews/raw-planning-minimax-m3-2026-06-05.md:1` (0 bytes), `tasks.md:T005`, `AGENTS.md` "empty/hung/malformed… do not count toward the three" | Run an explicit enabled non-GPT replacement lane (e.g., `openrouter/deepseek/deepseek-v4-flash`, `openrouter/~moonshotai/kimi-latest`, or `openrouter/xiaomi/mimo-v2.5-pro` from the roster) and record its disposition before Phase 2. | Open |
| F2 | **Major** | Product/scheme drift (vocab reuse) | Spec 084 introduces a parallel "role" classification vocabulary (`producer_candidate`, `ux_pattern_source`, `ready_for_import_planning`, `blocked`, `rejected`) in `data-model.md:11-44` and `contracts/external-tool-profile.md:7-16`, but the existing `internal/producerfamily` package already defines a `Decision` enum (`accepted`, `narrowed`, `rejected`, `blocked`, `not_assessed`) plus `SupportState` (`candidate_only`, `accepted`, `narrowed`, `rejected`, `blocked`) consumed from `producer-evaluations.jsonl` via `detectProducerEvaluations` (`contextprep.go:546-596`). Spec 053 ratified the JSONL evaluation record as the canonical machine-readable adoption-decision surface (`internal/testfixtures/language-agnostic-producers/producer-records.jsonl` already uses `producer-evaluation` with `Decision=Narrowed`, etc.). Spec 084's role vocabulary is a parallel scheme with no explicit mapping or decision to remain markdown-only. | `internal/producerfamily/producerfamily.go:118-170`, `internal/contextprep/contextprep.go:546-596`, `internal/testfixtures/language-agnostic-producers/producer-records.jsonl:1-3`, `docs/specs/084-external-tool-evaluation-profiles/data-model.md:10-44` | Either (a) state in `plan.md` and `research.md` that the profile document is intentionally a human-readable companion to, and not a replacement for, the producer-evaluation JSONL record, and add a follow-up note that future importer specs (P6-085, P6-086) will emit `producer-evaluation` records per candidate; or (b) drop the parallel `role` enum and reuse the existing one. Either path needs a sentence in the spec, plan, and contract. | Open |
| F3 | **Major** | Backlog / surface inconsistency | `product-backlog.md:168` (P6-084) still reads `"Draft spec; backlog-only. No plan/tasks yet."` while `spec.md:7-8` claims `"Ready for implementation. `plan.md` and `tasks.md` exist…"`. Spec/backlog/task ledgers must agree before T020 closeout. | `docs/product-backlog.md:168` vs `docs/specs/084-external-tool-evaluation-profiles/spec.md:7-8` | Update the backlog row to "Plan + tasks exist; planning review in progress; not yet ready for merge" in the same commit that updates `spec.md` status. Cover during T020. | Open (covered by T020) |
| F4 | Minor | Requirements drift | FR-007-009 are name-bound to three specific repos; if any of those move, the FRs require edits. Plan and tasks T008-T010 mirror that binding. | `spec.md:122-133`, `tasks.md:T008-T010` | Accept as a maintenance cost, but record in the contract that FR-007-009 are bound to current repo identities and a rename/transfer triggers a profile refresh. | Accept |
| F5 | Minor | Constitution drift | None. The plan's constitution check passes all five principles; the slice adds no installs, network, daemon, mutation, credentials, schema, or graph evidence promotion. | `plan.md:60-74`, `.specify/memory/constitution.md:6-42` | Accept. | Accept |
| F6 | Minor | Product drift | Scope is bounded to documentation + bounded context guidance, matching `docs/product-boundary.md:25-39` and `docs/product-claims.md` "Portolan composes existing OSS tool outputs" row. | `plan.md:13-20`, `product-boundary.md:25-40`, `product-claims.md:55-65` | Accept. | Accept |
| F7 | Minor | Evidence-state honesty | FR-004 and SC-002 explicitly prohibit promoting candidate profiles to observed evidence. T006 + T014 add focused test coverage. Existing `contextprep` already keeps candidate tools at `not_assessed` / `unknown` in `oss-plan.json` and `evidence-index.jsonl`. | `spec.md:114-116,150-152`, `tasks.md:T006,T014`, `contextprep.go:1437-1462` | Accept. | Accept |
| F8 | Minor | Privacy / safety boundaries | FR-003, FR-005, and edge cases cover target mutation, daemon/watch, credential, and LLM-authored summary risks with explicit approval gates. | `spec.md:111-119,90-99`, `contracts/external-tool-profile.md:26-32` | Accept. | Accept |
| F9 | Minor | Task testability | Each phase names concrete files, independent tests, and verification commands. Test-first discipline holds for behavior changes (T006 → T013 → T014). | `tasks.md:T006,T012,T015,T019-T023`, `plan.md:123-136` | Accept. | Accept |
| F10 | Minor | Spec assumption vs plan behavior | Spec assumption says "documentation and guidance first; it does not add an importer, execute external tools, install dependencies, or change schemas." But T013 changes `internal/contextprep/contextprep.go` (a Go code change) and T014 updates tests. The plan correctly notes the bounded exception in "Project Structure" and the constitution check, but the spec's framing is slightly misleading. | `spec.md:165-166` vs `tasks.md:T013-T014`, `plan.md:108-114` | Add one clarifying sentence to `spec.md` "Assumptions" that "Bounded context-pack guidance may add a small pointer in `internal/contextprep`; this is not an importer, schema change, or graph evidence fact." | Fix in same commit as status update |
| F11 | Minor | T022 review lens mandate vs docs-heavy diff | T022 mandates CRAP / Maintainability Index / CleanArch-hex / CleanCode / SOLID / DRY / YAGNI for the PR. The slice is dominated by new `docs/adapter-contracts/external-tool-evaluation-profiles.md` (docs), bounded T013 in `internal/contextprep` (Go), and T006/T014 in `internal/contextprep/contextprep_test.go` (test). Per `AGENTS.md` "For docs/config-only PRs, record code metrics and code-architecture lenses as `not_applicable` with diff evidence." Since the diff does touch Go, lens `not_applicable` is only partial. | `tasks.md:T022`, `AGENTS.md` PR review section, `internal/contextprep/contextprep.go` (T013 touches ~10-30 lines) | Reframe T022 to: "Run quality lenses; for the new `docs/adapter-contracts/external-tool-evaluation-profiles.md` and the bounded `internal/contextprep` change, classify each lens against the touched file set; record CRAP/MI/CleanArch-hex as `not_applicable` for the docs file with diff evidence, and apply the lenses to the small `internal/contextprep` change." | Fix wording in T022 |
| F12 | Minor | Branch metadata agreement | `spec.md:3`, `plan.md:3`, the actual git branch (`git status --short --branch` → `codex/084-external-tool-evaluation-profiles`), and the `AGENTS.md` plan pointer (now updated) all agree on `codex/084-external-tool-evaluation-profiles`. The original spec.md branch field `codex/084-tool-adoption-specs` is now corrected. | `spec.md:3`, `plan.md:3`, `AGENTS.md` SPECKIT block, `git status` | Accept; spec.md branch rename is already in the working tree. | Accept (in working tree) |
| F13 | Minor | Cross-spec coordination | Spec 084's classifications (ast-index strongest, CodeGraph lower-fit, Understand-Anything UX-only) directly feed P6-085 (ast-index producer import) and P6-086 (evidence navigation UX patterns), which are still backlog-only drafts. No cross-reference is recorded in spec 084. | `docs/product-backlog.md:168-170` | Add a one-line "Related future slices" note at the bottom of `spec.md` pointing to P6-085 and P6-086 with the planning note that they remain backlog-only. | Fix in same commit |
| F14 | Minor | Snapshot freshness callout | `research.md:32-38` is dated 2026-06-05 and the spec assumption says upstream may change after 2026-06-04. The `last_refreshed` field in `data-model.md:7-9` handles this at profile-record level, but the spec/plan do not name when the next mandatory refresh happens (e.g., before PR ready or before any future importer spec). | `research.md:32-38`, `spec.md:165-168`, `data-model.md:7-9` | Add a line in `plan.md` "Verification" or `quickstart.md` "Verify Profile Guidance" naming the refresh step as required before PR ready. | Fix in same commit |
| F15 | Minor | Quickstart command sequence | `quickstart.md:20-23` says to run `go run ./cmd/portolan context prepare --root . --out /tmp/portolan-084-context --force` and `jq empty /tmp/portolan-084-context/oss-plan.json`. The existing `contextprep` test pattern uses `out := filepath.Join(root, ".portolan", "context")` and T019 is a baseline run; the quickstart's `--out /tmp/...` is fine for humans but the test (T006) will likely use a `t.TempDir()`-style path. The quickstart is correct as a human smoke, just confirm it does not need to also assert the contract profile file exists. | `quickstart.md:18-24`, `tasks.md:T006` | Accept; quickstart is human-oriented. T006 owns the assertion contract. | Accept |
| F16 | Minor | T005 "planning review" naming | T005 is the disposition for "three assessed non-GPT planning review lanes." The current `raw-planning-*.md` files are raw model output, not the disposition file. T005 needs to produce `reviews/planning-review-disposition-2026-06-05.md` summarizing the three lanes. F1 must be fixed before that disposition can be written. | `tasks.md:T005` | Bundle F1 fix + T005 disposition into the same Phase 1 step. | Bundle with F1 |

## Cross-cutting assessment

- **Requirements drift**: Mostly clean. FR-001-FR-010 are concrete, bounded to documentation/guidance, and traceable to T006-T018. Only F4 (name-binding) and F10 (assumption vs Go change) flagged.
- **Constitution drift**: None. Plan's gate passes all five principles; slice adds no install/network/daemon/mutation/credentials/schema/evidence promotion.
- **Product drift**: Bounded and aligned with `product-boundary.md` and `product-claims.md`. F2 is the one real scheme-reuse concern.
- **Evidence-state honesty**: Held. Existing `contextprep` already routes candidate tools through `not_assessed` and `unknown` paths; T006 + T014 add test coverage.
- **Privacy / safety boundaries**: Held. Approval-gated execution/install/MCP/watcher/daemon/mutation requirements are explicit and the plan refuses to add a native producer wrapper.
- **Task testability**: Held. Each phase names files, independent tests, and verification commands; baseline commands in `AGENTS.md` are reused.

## Verdict

**CHANGES_REQUESTED** (3 major + 11 minor / acceptable)

Blocking items before T020 closeout:

1. **F1**: Run a third assessed non-GPT planning review lane (M3 produced an empty 0-byte file and does not count) and write `reviews/planning-review-disposition-2026-06-05.md` summarizing three assessed lanes. Until then, T005 is not complete and AGENTS.md "empty… do not count toward the three" applies.
2. **F2**: Decide between (a) explicit companion-document framing with a future-spec note that P6-085 / P6-086 will emit `producer-evaluation` records for the three candidates, or (b) reuse the existing `Decision` / `SupportState` enums and drop the parallel `role` vocabulary. Document the decision in `plan.md` and `contracts/external-tool-profile.md`.
3. **F3**: Reconcile backlog row P6-084 with `spec.md` status (do this in T020, but lock the wording now).

Minor items F4-F16 can be folded into the same Phase 1 commit and the T020 closeout.

## not_assessed

- M3 lane review output (empty file → `not_assessed` per AGENTS.md and `docs/review-harness-benchmark.md:130-138`).
- T006 / T013 / T014 implementation behavior (no code changed in this review; tasks are unchecked).
- GitHub checks / PR state / merge readiness (pending Phase 6).
- Post-2026-06-05 external tool upstream metadata drift (handled by `last_refreshed` field; covered by F14).
- Real `ast-index` / `codegraph` / `understand-anything` output acquisition — explicitly out of scope per `spec.md:165-166` and P6-085 / P6-086 backlog-only status.
