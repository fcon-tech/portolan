Let me first read the spec and understand User Story 2, then check review tooling availability.
Now let me read the rest of contextprep.go (the rendering functions) and check the external tool profiles doc:
Now let me check tooling preflight and build the review context:
Good - all three models and the explore agent are available. Now let me build the review context:
Now let me wait for all three lanes and collect results:
All three lanes completed. Let me collect their results:
All three lanes completed. Let me verify the key findings locally:
All evidence collected. Here is the synthesized review:

---

## Mode: REVIEW

**review status**: partial
**scope**: local uncommitted diff + smoke-tested generated context pack (`/tmp/portolan-084-context/`)
**lanes**:
- requirements / zai-coding-plan/glm-5.1: **findings** (3 findings, 0 blocking)
- code / kimi-for-coding/k2p6: **pass**
- security / minimax/MiniMax-M2.7: **pass**

---

### Findings

**F-001 — medium: FR-010 stale-profile risk not surfaced in generated `answer-contract.md`**
- **Requirement**: FR-010: "Generated guidance MUST make stale-profile risk visible and require a fresh upstream/local verification step before implementation depends on a candidate's current behavior."
- **Evidence**: The profile doc (`docs/adapter-contracts/external-tool-evaluation-profiles.md:32-33`) contains a one-day refresh rule, but the generated `answer-contract.md` paragraph at `contextprep.go:2434` delegates to the profile doc without surfacing staleness risk. A generated context pack (`/tmp/portolan-084-context/answer-contract.md`) confirmed: the word "stale" appears only for artifact boundaries, never for tool profile metadata staleness. The test (`contextprep_test.go:205-215`) does not assert stale-risk language in `answer-contract.md`.
- **Recommendation**: Add a one-line stale-risk reminder to the answer-contract paragraph (e.g., "Profile metadata may be stale; refresh upstream verification before depending on current tool behavior.") and add a test assertion for it.

**F-002 — low: Case-sensitive evidence-index forbidden check**
- **Evidence**: `contextprep_test.go:219` forbids `"codegraph"` (lowercase) in `evidence-index.jsonl`. The answer-contract text uses `"CodeGraph"` (capitalized). If any future code path wrote the capitalized form, the test would miss it. Verified locally: `"CodeGraph"` does not appear in the evidence index. Practical risk is currently zero because `contextprep.go:2434` writes tool names only to `answer-contract.md`, not to the evidence index.
- **Recommendation**: Change `"codegraph"` to `"CodeGraph"` in the forbidden list or use `strings.ToLower` before checking.

**F-003 — low: T014 test scope narrower than task description**
- **Evidence**: T014 says "assert ... no external tool execution command is emitted." The test (`contextprep_test.go:218-223`) only checks `evidence-index.jsonl` exclusion. It does not inspect `oss-plan.json` or other generated files for execution commands. The implementation is correct (no execution commands are added), but the test does not fully cover the T014 acceptance claim.
- **Recommendation**: Add a negative assertion on `oss-plan.json` or narrow T014 description to match actual test scope.

---

### Verified Requirements

| Requirement | Status | Evidence |
|---|---|---|
| US2 AS1 (no graph facts from candidates) | **pass** | `contextprep.go:2434` explicitly forbids promotion; test verifies `evidence-index.jsonl` excludes tool names |
| US2 AS2 (stale-profile risk) | **partial** | Profile doc has stale rules; generated `answer-contract.md` does not surface them (F-001) |
| FR-004 (no evidence promotion) | **pass** | Guidance says "Do not promote candidate profiles to source-visible, metadata-visible, or runtime-visible evidence" |
| FR-005 (approval boundaries) | **pass** | Profile doc records approval gates for all three candidates |
| FR-010 (stale risk visible) | **gap** | Delegated to profile doc only; not in generated guidance (F-001) |
| SC-002 (no graph fact from candidate) | **pass** | evidence-index.jsonl exclusion confirmed by test and local smoke |
| Evidence-state honesty | **pass** | Text positions profiles as guidance, not evidence |
| Privacy/security | **pass** | No secrets, network access, credentials, or command execution in the diff |

### Evidence gaps
- CI/CD checks: `not_assessed`
- PR review approval: `not_assessed`
- `oss-plan.json` negative assertion for external tool commands: `not_assessed`

### Conflicts
- Lane 1 (requirements) found 3 findings; Lane 2 (code) and Lane 3 (security) found no defects. No disagreement — the requirements lane applied a stricter FR-010 lens that the code and security lanes did not specifically test against.

---

**Verdict**: **CHANGES_REQUESTED** — F-001 is a medium-severity gap against FR-010. The generated `answer-contract.md` should surface stale-profile risk rather than delegating it entirely to the profile document. F-002 and F-003 are low and can be addressed alongside F-001.
- merge_approval: `not_assessed`
