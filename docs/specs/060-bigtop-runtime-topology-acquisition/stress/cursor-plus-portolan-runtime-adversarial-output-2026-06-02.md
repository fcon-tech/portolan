Adversarial evaluation against Portolan stress probe `20260602-060-runtime-topology`, `selection.json`, and producer outputs. Per `answer-contract.md` and `agent-brief.md`, **runtime topology** requires `runtime-visible` observations; Helm, Compose, ctags, and protobuf outputs are **metadata-visible** or static and must not be promoted to live Bigtop runtime topology.

| adversarial_claim | accept_or_reject | status | reason | evidence_needed_to_accept |
| --- | --- | --- | --- | --- |
| The Helm templates prove Alluxio/Bigtop runtime topology. | **reject** | **partial** | `helm template` produced static YAML under `.portolan/stress/20260602-057-producer-expansion/tool-outputs/` for **Alluxio charts only** (4 `verified`, 1 `blocked` on `alluxio-job`). That is `deployment-model` / `metadata-visible` intent in the `alluxio` repo, not a running cluster. `selection.json` has `"runtime": null`; probe reports `bigtop_kubernetes_pod_matches: 0` and `bigtop_kubernetes_service_matches: 0`; no Bigtop/Hadoop/Alluxio names in `kubectl-pods.txt` / `kubectl-services.txt`. | Time-bounded **runtime-visible** evidence: Bigtop/Alluxio workloads actually running (pods/services/containers), plus observed communication (ports, DNS, traces, or a dedicated runtime-observation producer) tied to those workloads—not `helm template` render output alone. |
| The minikube node is a Hadoop/Bigtop runtime. | **reject** | **partial** | `docker-ps.tsv` shows a `minikube` container (`gcr.io/k8s-minikube/kicbase`); `runtime-probe-summary.json` has `kubernetes_context: "minikube"`. That verifies a **local Kubernetes control plane**, not a Hadoop/Bigtop stack. Running containers are Faust, a bot, Tika, and minikube—**no** Bigtop image/service. `bigtop_docker_matches: 0`; K8s grep over probe outputs has **no** bigtop/hadoop/alluxio matches. Process list “bigtop” hits are probe shell paths, not NN/DN/YARN/HDFS daemons. | Runtime-visible proof that minikube (or another target) hosts **Bigtop/Hadoop components** (e.g. NameNode, DataNode, ResourceManager) with identity and health checks, and optionally dependency edges between them during the capture window. |
| Universal Ctags found millions of symbols, so runtime topology is verified. | **reject** | **verified** | `ctags-summary.json` reports **5,390,732** tags across **93,380** files—static **symbol-index** coverage of selected repos. Many hits are code identifiers (e.g. `flink-runtime`, `connect/runtime`), not operational services. Portolan gaps keep **runtime-observation** `not_assessed` for all repos; symbol-index does not satisfy `runtime-visible` in the answer contract. | A **runtime-observation** producer output classifying live processes/containers/pods and observed inter-service communication; symbol counts may supplement scope but cannot substitute for runtime topology. |
| Docker Compose config proves the live Bigtop service dependency graph. | **reject** | **partial** | `apache-bigtop-compose.config.json` (initial-proof tool output) parses **one** declared `bigtop` service (`apache/bigtop-placeholder`, bind-mounts into `apache-bigtop-repo`)—declared dev topology, not a live graph. **`docker ps` has no Bigtop container**; `bigtop_docker_matches: 0`. `selection.json`: `"runtime": null`, `"tool_outputs": null`. | **Running** Bigtop Compose (or equivalent) stack, runtime-visible container list and port/link observations, and traced dependencies during capture—e.g. `docker compose ps`, service health, and network/process evidence showing which services actually talk to which. |

### Cross-cutting conclusion

**Bigtop runtime topology on this host is `not_assessed` / `cannot_verify` for live operation.** What is well supported today is **static landscape evidence** (repos, Helm render, Compose config, ~5.4M ctags symbols, jscpd/protobuf artifacts) and **partial** proof that **minikube and unrelated Docker apps** exist—not that the Bigtop/Hadoop/Alluxio stack is up or wired as production topology.

**Smallest read-only next step** (per answer contract): re-run or inspect runtime probes after starting the intended Bigtop stack, or attach a `runtime-observation` producer output to `selection.json` (`runtime` / `tool_outputs` non-null) and re-validate with `portolan selection validate` + map/context refresh.
