# Tasks: Configuration Surface Detection

- [x] T001 Add local fixture with env refs, port declarations, container files,
  workflow files, manifests, feature flags, and secret references without
  private payloads.
- [x] T002 Add focused tests proving `portolan map` emits observed
  configuration findings and graph nodes with redacted evidence.
- [x] T003 Implement bounded native configuration surface detection.
- [x] T004 Integrate configuration graph nodes and findings into root and
  selection map runs while preserving `not_assessed` when no surfaces exist.
- [x] T005 Update backlog, agent docs, and product hypothesis ledger.
- [x] T006 Run baseline checks, fixture smoke, secret-leak check, review
  disposition, and commit.
