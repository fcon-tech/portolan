# Plan: Viewer Graph & Search Polish (099)

- `viewer/src/index.html` + `app.js` + `styles.css`: Graph hints tab via `/api/evidence-index`
- `scripts/build-search-index.sh`: rg path + head fallback
- `scripts/build-portolan-bundle.sh`: gap when rg missing
- Overview blocks from landscape-report sections
- Demo runbook + README
