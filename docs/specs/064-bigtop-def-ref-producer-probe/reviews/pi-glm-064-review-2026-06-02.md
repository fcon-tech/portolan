# PI Review Lane: GLM 5.1

Date: 2026-06-02
Model: `zai/glm-5.1`
Harness: `pi --no-tools --no-context-files --no-session`

## Verdict

assessed:

- Approved / PR-ready with minor advisories.
- Definition/reference boundary is maintained.
- `cannot_verify` is consistent across spec, plan, ledger, and Cursor stress.
- Build/indexer approval boundary is explicit.

## Findings

minor:

- `gradle` should be explicitly listed as absent from PATH.
- Empty output file hashes are valid evidence of empty probe result.
- `jdeps-cachedir.txt` content should be represented accurately.
- Exit code `0` from `jdeps` should be described as tool functionality, not
  project graph evidence.
- Probe scope should be explicitly bounded to the three selected repos.

## Disposition

accepted and fixed, then refined after DeepSeek review:

- Rewrote tool availability as explicit `found` / `not_found` rows.
- Kept `gradle` as an absent build tool, not a def/ref indexer.
- Added exact `jdeps` captured output and interpretation that exit code `0`
  proves tool functionality only, not project graph evidence.
- Added selected-scope boundary for the three probe roots.
- Added explicit note that zero-byte hashes for `class-files.txt` and
  `target-classes-dirs.txt` prove the probes ran and found no compiled
  artifacts.

no change:

- Empty file hashes are correct evidence that no class files or target class
  directories were found.
