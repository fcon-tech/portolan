# Plan: Agent Analysis Claims (106)

- Schema + importer (node, reuses bundle-query loaders for ref resolution).
- bundle-query family `claims`; MCP tool; SKILL + guardrail.
- evidence-model.md tier subtypes; smoke: valid import + rejected broken-ref claim.
