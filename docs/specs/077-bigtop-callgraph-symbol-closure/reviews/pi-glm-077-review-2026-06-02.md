# Independent PR Review: Spec 077 — Bigtop Callgraph And Symbol Closure

**Reviewer**: Lane 2 independent non-GPT review
**Date**: 2026-06-02
**Packet scope**: `docs/specs/077-bigtop-callgraph-symbol-closure/` — decision-record work only, no Go code changes

---

## Findings

### F1. Requirements Fit — **minor**

The spec and plan correctly own the full symbol/reference/call-graph gap created by spec 075. FR-001 through FR-005 are directly traceable to plan steps and tasks. The decision gate explicitly rejects jumping to spec 076 parity with an open gap.

One minor observation: FR-001 says "identify mature local-first, read-only producers or build outputs that can emit resolved symbol, reference, dependency, or call edges." The producer comparison table covers this, but **build outputs** (as opposed to build-tool exporters) are not separately assessed. The availability ledger shows `mvn` and `java` are present; prior spec 075 ledgers already bounded what those produce. This is not a gap requiring rework — just note that the "build outputs" arm of FR-001 is resolved by reference to prior specs rather than a dedicated subsection.

**Severity**: minor
**Evidence**: spec.md FR-001 vs. producer-decision record table structure
**Recommendation**: Accept as-is. Prior spec ledgers close the build-output arm implicitly.

---

### F2. Product-Boundary Fit — **no issue**

The plan, availability ledger, and decision record all enforce the local-first/read-only boundary:
- No tool installation attempted
- No network access used
- No target repository mutation
- No runtime service started
- No credentials or external provider contact

The `cannot_verify` classification for missing producers is the correct constitutional response. The producer comparison table is thorough — eight candidate families plus three already-bounded tools, all assessed against local-first constraints.

---

### F3. Evidence-State Honesty — **no issue**

This is the strongest aspect of the packet.

The decision record and all supporting artifacts rigorously preserve the evidence-state hierarchy:
- Existing Ctags/gopls/jdeps outputs remain `partial` — no upgrade attempted
- Full C6/callgraph is explicitly `cannot_verify`
- Cursor+Portolan enterprise parity is `cannot_verify` (correctly deferred to spec 076)
- The availability ledger states verified/cannot_verify/blocked boundaries without collapsing unknowns into success
- The cursor stress output preserves `cannot_verify`, `not_assessed`, and `partial` labels and refuses to stack partial evidence into a full claim

No evidence-state inflation detected.

---

### F4. Local-First / Read-Only Safety — **no issue**

The availability ledger explicitly documents "read-only PATH/version checks only" and enumerates exactly what was not done (no installs, no network, no mutation, no runtime startup). The decision record correctly treats tool absence as a valid outcome rather than a defect to work around. The blocked-pending-separate-approval section names exact future actions that would require new approval boundaries.

---

### F5. OSS Composition Posture — **no issue**

Constitution III (complement, do not replace) is well observed:
- Seven candidate mature OSS producers are evaluated before any native implementation
- Native Portolan graph extraction is explicitly rejected with constitutional justification
- The comparison table documents fit, current state, risk/blocker, and decision per producer
- Source notes cite Sourcegraph, CodeQL, JDT LS, and srcML documentation for external claims

---

### F6. Test / Verification Gaps — **no issue (decision-record only)**

The plan correctly states: "For docs-only or decision-record-only closure, no additional Go tests are required beyond baseline." The verification section names `go test ./...`, `go vet ./...`, `jq empty schema/*.json`, and `git diff --check` as baseline checks. These are appropriate for a decision-record-only slice. No code changes are present in this packet, so CRAP, MI, CleanArch, SOLID, and runtime metrics are **not_applicable**.

---

### F7. Full Symbol/Reference/Callgraph Claims — **cannot_verify (correct)**

The packet explicitly and correctly classifies these as `cannot_verify`:
- Full Bigtop symbol/reference graph: `cannot_verify`
- Bigtop call graph: `cannot_verify`
- Full C6/callgraph closure: `cannot_verify`

No resolved graph evidence exists in this packet. The absence of safe producers is itself the verified finding. The cursor stress output independently confirms this classification under adversarial prompting. This is constitutionally honest — unknown/unverifiable facts are valid outputs, not errors to hide.

---

### F8. Open Tasks T014–T019 — **not_assessed (expected)**

T014 (three assessed independent non-GPT review lanes) through T019 (merge closeout) remain open. This is expected per the task list and task checkpoint structure. This review constitutes lane 2 of T014; lanes 1 and 3, plus all closeout tasks, are pending. These open tasks are not product design defects.

**Severity**: not_assessed
**Recommendation**: Complete T014–T019 per task list before PR merge.

---

### F9. Decision Record Confidence and Reversibility — **no issue**

The producer decision record states `confidence: high` and `reversibility: high` with clear upgrade path: "a later slice can upgrade the claim if a mature producer is approved, installed, run, ledgered, and independently reviewed." The four-step upgrade criteria (approve, install, run, independent review) are explicit and prevent silent claim inflation in future slices.

---

### F10. Cursor Stress Design — **minor**

The cursor stress prompt is well-structured: it presents facts only, prohibits inference, and asks six targeted claim-boundary questions. The output preserves all required evidence-state labels.

Minor observation: the stress prompt does not include the exact task status (T010–T013 checked, T014+ open). This would have given Cursor additional context on what is still pending vs. decided. However, the prompt is scoped to claim boundaries, not process status, so this is acceptable.

**Severity**: minor
**Recommendation**: Accept as-is.

---

## Verdict

**Accept with no blocking findings.**

The packet is clean, constitutionally honest decision-record work. It correctly identifies producer absence, refuses to inflate partial evidence into full claims, and gates future upgrades behind explicit four-step criteria. All `cannot_verify` classifications are warranted by the evidence. Open tasks T014–T019 are expected and not defects.

---

## Not Assessed

| Item | Reason |
| --- | --- |
| Go code changes | None present in packet |
| CRAP / MI / CleanArch / SOLID metrics | Not applicable; decision-record only |
| Runtime UI / code architecture | Not applicable |
| Spec 074 runtime health execution | Outside 077 scope; approval-gated |
| Spec 076 enterprise parity | Deferred; depends on 077 completion |
| Lanes 1 and 3 of T014 | This is lane 2 only |
| T015–T019 closeout tasks | Not yet started |
| Actual graph producer output | No safe producer available; `cannot_verify` |
| GitHub review approval state | `not_assessed` per prior backlog convention |

---

## Summary Table

| Finding | Severity | Disposition |
| --- | --- | --- |
| F1: Build-outputs arm of FR-001 implicit | minor | Accept as-is |
| F2: Product-boundary fit | no issue | — |
| F3: Evidence-state honesty | no issue | Exemplary |
| F4: Local-first/read-only safety | no issue | — |
| F5: OSS composition posture | no issue | — |
| F6: Test/verification gaps | no issue | Baseline appropriate |
| F7: Full graph claims | cannot_verify | Correct classification |
| F8: Open tasks T014–T019 | not_assessed | Expected; not defects |
| F9: Decision record confidence | no issue | — |
| F10: Cursor stress prompt scope | minor | Accept as-is |
