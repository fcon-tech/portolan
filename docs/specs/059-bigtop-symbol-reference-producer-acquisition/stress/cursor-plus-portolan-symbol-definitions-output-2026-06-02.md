## Updated C6 (Spec 059 — Universal Ctags symbol definitions)

Prior baseline (specs 054–058): **C6** was `partial` with only bounded `gopls` symbol listing on five Airflow Go SDK files; `producer-run-bigtop-symbol-index-not-assessed-20260601` and `gap-symbol-index-not-assessed` blocked symbol/reference and call-graph claims.

| criterion | updated_status | allowed_claim | forbidden_claim | remaining_gap |
| --- | --- | --- | --- | --- |
| **C6 — Symbol/reference graph** | **partial** (definitions catalog only; reference graph still **not_assessed**) | For the **15 repos in `selection.json`**, Cursor + Portolan may claim a **local, source-visible symbol-definition catalog** from producer run `producer-run-bigtop-selected-universal-ctags-20260602` (Universal Ctags 6.2.1): **5,390,732** `def` tags, **93,380** unique files, **0** bad JSONL lines, dominant role `def`, spanning Java, JSON, Python, JavaScript, C++, SQL, PHP, TypeScript, Protobuf, Go, Maven2, Thrift, shell, etc. (per `ctags-summary.json` / stress 059). This supports bounded “where is X defined?” lookups within selected scope—not enterprise-wide navigation. Prior **gopls** slice remains valid as a narrower supplement. | Full **symbol/reference graph**, **call-site / use-site** navigation, **cross-repo references**, **complete call graph**, “what calls what” across the JVM/polyglot stack, Sourcegraph-class code intelligence, or treating tag volume as semantic architecture coverage. Do **not** promote ctags output to `symbol-index` / `runtime-visible` without a def+ref producer (SCIP, LSIF, etc.). Do **not** claim coverage of the three on-disk repos outside `selection.json` (e.g. livy, oozie, sqoop). | **Reference edges** and queryable def/ref index remain **not_assessed** (`gap-symbol-index-not-assessed`, blocked claims: symbol/reference relationships, complete call graph). No ingestion of 059 into canonical `producer-runs.jsonl` / map `finding-relationships-symbol-evidence-not-assessed` yet. Ctags is **definitions-only** (`roles: def`); JSON/config tags inflate counts; precision/semantic quality unverified vs SCIP/LSIF. **C4** runtime topology unchanged (**not_assessed**). **C9** enterprise code-intelligence parity unchanged (**not_assessed**). |

### Direct answers

1. **What can Cursor + Portolan claim now?** A **bounded, multi-language symbol-definition inventory** for selected Bigtop targets—file/path/line/kind/name definitions at scale, evidence-backed by the 059 producer run—not a reference or call graph.
2. **What must remain forbidden?** Any claim of **full symbol/reference graph**, **call graph**, **cross-repo “who uses this”**, or **enterprise code-intelligence parity**; also forbidden: inferring **runtime topology** from this evidence.
3. **Does this change runtime topology?** **No.** Bigtop runtime topology stays **`not_assessed`**; ctags is static, source-visible definition extraction only.
4. **Does this verify enterprise code-intelligence parity?** **No.** Enterprise parity stays **`not_assessed`**; C6 improves from a tiny gopls slice to a broad **definitions-only** slice, but C6’s reference-graph requirement is still unmet and C4 is still open.

**Verdict:** Spec 059 **narrows** C6 from “five Go files only” to “selected-scope definition catalog at scale,” but C6 stays **`partial`** and enterprise parity **cannot** be claimed until a def+ref producer and runtime-visible evidence exist.
