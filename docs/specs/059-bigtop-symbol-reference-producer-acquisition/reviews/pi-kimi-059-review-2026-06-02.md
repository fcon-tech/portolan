## Spec 059 Review Findings

| # | Severity | Finding | Evidence | Recommendation | Verdict | Not Assessed |
|---|----------|---------|----------|--------------|---------|-------------|
| 1 | **info** | Evidence-state honesty preserved | C6 updated only to `partial`; `metadata-visible` label applied; `def` role (5,390,211/5,390,732) explicitly noted | Continue requiring role-level breakdown in all producer ledgers | ✅ Honest | — |
| 2 | **low** | "Symbol/Reference" in spec name oversells | Spec title, branch name, and backlog row all say "symbol/reference producer acquisition"; actual output is definitions-only | Rename future specs to "symbol-definition producer" when scope is def-only; or add explicit scope qualifier in title | ⚠️ Overclaim risk contained by ledger, but naming invites misreading | Full def/ref graph |
| 3 | **medium** | `metadata-visible` may be generous for ctags | Ctags reads file content and infers structure heuristically; no source AST or type system verification is performed | Consider `metadata-visible` correct for file+line+name extraction, but document that semantic accuracy is unverified; add `claim-only` sub-label for inferred tag kinds | ⚠️ Acceptable with caveat | Semantic correctness of tag classification |
| 4 | **low** | 5.4M tags / 93K files output size risk | JSONL at ~5.4M records; stress root is local and uncommitted, but repeated runs accumulate | Add retention policy to stress root; document output size bounds in spec; consider compression or sampling for repeatability tests | ✅ Mitigated by local-only, uncommitted storage | Long-term storage cost |
| 5 | **medium** | No target-mutation audit trail | FR-002 forbids mutation; ledger asserts "target repos not mutated" but no pre/post hash or readonly mount evidence is recorded | Add lightweight target-mutation check (e.g., `git status --short` in each repo, or readonly bind-mount note) to future acquisition ledgers | ⚠️ Trust-based; strengthen next spec | Formal target-integrity proof |
| 6 | **info** | Cursor stress correctly gated | C4 runtime and C9 enterprise parity remain `not_assessed`; no forbidden claims appear in accepted output | Maintain this pattern; add explicit C4/C9 restatement in every rubric update template | ✅ Correct | — |
| 7 | **medium** | SC-003 partially satisfied | Output classified as "definitions-only" but classification method is human review of role distribution, not automated `ref` edge detection | Add automated check: assert zero or near-zero `ref`/`call` roles in ctags output, or explicitly state that ctags format lacks reference fields | ⚠️ Good enough for now; automate next time | Automated def/ref discrimination |
| 8 | **low** | T016 independent review incomplete | Tasks show T016 unchecked; no second-lane review evidence is present | Complete T016 before PR merge; or record explicit waiver with rationale | ⚠️ Open | Independent review disposition |
| 9 | **info** | Reversibility claim valid | Homebrew install, external output path, no Portolan code change | Document uninstall/rollback command in ledger for reproducibility | ✅ Sound | — |
| 10 | **low** | FR-006 blocker path not exercised | All tasks checked green; no `cannot_verify` or blocked-acquisition branch is demonstrated | For future specs, include a negative-test task: simulate or document what happens when acquisition fails (e.g., no Homebrew, network disabled) | ⚠️ Not required for this slice | Failure-mode coverage |

### Bottom Line

Spec 059 **moves the objective forward honestly**: C6 advances from `not_assessed` to `partial` with real, bounded, local, read-only evidence. The definitions-only nature is **not hidden**—it is repeatedly labeled and the "symbol/reference" naming tension is **contained by the ledger**. Runtime and enterprise parity remain correctly unclaimed. The main gaps are incomplete independent review (T016) and the absence of an automated def/ref discrimination check.
