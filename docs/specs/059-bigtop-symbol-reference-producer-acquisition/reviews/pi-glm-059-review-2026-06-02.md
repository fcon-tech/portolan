# Spec 059 Review: Bigtop Symbol/Reference Producer Acquisition

| # | Finding | Severity | Evidence | Recommendation | Verdict |
|---|---------|----------|----------|----------------|---------|
| 1 | **Ctags acquisition safety is sound** | ✅ OK | Homebrew user-local install under `/home/linuxbrew/`; output written to `.portolan/stress/` outside target repos; ledger confirms no target mutation, no credentials, no network calls. `ctags` is read-only by design (parses files, writes elsewhere). | Maintain the documented boundary. | Passed |
| 2 | **Definitions-only correctly labeled, not overclaimed as symbol/reference** | ✅ OK | Producer decision explicitly states "Definitions/tags, not references." Run ledger limitation column says "not a full definition/reference graph." C6 update uses "partial" not "verified." Rubric update has explicit forbidden-claims section. FR-004 and FR-005 are honored. | No change needed. | Passed |
| 3 | **Evidence-state honesty is strong** | ✅ OK | Run classified as `metadata-visible` (not `source-visible` or `runtime-visible`), which accurately describes parsed-tag metadata rather than provenance-traced source or live runtime. 5.3M tags with `bad_json_lines=0` is a real validation metric. Warnings about Puppet/JS parser gaps are recorded, not hidden. | Continue recording parser warnings per language if further runs occur. | Passed |
| 4 | **Privacy/output-size risk partially addressed** | ⚠️ Minor | Privacy review column says "committed ledger stores summary only" — good. But the 5.3M-line JSONL at ~several GB is not size-bounded by policy. If this output were accidentally committed or exposed, it contains structural metadata (file paths, symbol names) across 15 repos. No `.gitignore` or exclusion rule for the JSONL is documented. | Add explicit `.gitignore` entry for `tool-outputs/*.jsonl` under the stress root, or document that the JSONL stays local-only by convention. Record estimated file size in the ledger. | Acceptable with mitigation |
| 5 | **Moves the original objective forward honestly** | ✅ OK | Backlog row P6-058 identified "full symbol/reference producer if safely available" as a gap. Spec 059 attacks this by acquiring the lowest-risk producer first, getting real evidence (5.3M tags, 93K files), and honestly classifying it as definitions-only. SCIP/LSIF family is explicitly deferred, not abandoned. The plan acknowledges ctags may be wrong and is reversible. | SCIP/LSIF deferral should have a concrete re-evaluation trigger (e.g., "when Go+Java LSIF indexers are available via Homebrew"). | Passed |
| 6 | **No pretending runtime/parity are solved** | ✅ OK | FR-007, C4 runtime, and C9 enterprise parity all remain `not_assessed` in every artifact. Cursor stress explicitly verified these were not upgraded. The spec, plan, and rubric update each repeat the non-claim boundary. | No change needed. | Passed |
| 7 | **GPL-2.0 license for ctags not flagged as project risk** | ⚠️ Minor | Producer decision records GPL-2.0-or-later but does not assess whether using ctags output (not linking) creates any obligation concern. In practice, running a GPL tool and consuming its text output does not infect Portolan (which is Go/BSD or similar). The risk is negligible but the silence is a gap. | Add one sentence to the decision record: "GPL applies to the ctags binary only; output consumption does not create copyleft obligation for Portolan." | Acceptable |
| 8 | **Scope boundary (15 targets from `selection.json`) is clear but arbitrary** | ℹ️ Info | The 15 selected targets are referenced but not listed inline. Reviewer must cross-reference `selection.json` to know what's covered. This is fine for local work but reduces standalone reviewability. | Consider inlining or summarizing the 15 target names in the ledger or spec. | Not assessed (style) |
| 9 | **Tasks T016-T017 incomplete** | ℹ️ Info | Final review lanes and PR closeout are unchecked. This is expected for "planning in progress" but the backlog row says all phases complete through T015. | Complete T016-T017 before marking the backlog row as merged. | Not assessed (pending) |

## Summary

| Category | Count |
|----------|-------|
| Passed | 5 |
| Minor (acceptable with mitigation) | 2 |
| Info / Not assessed | 2 |
| Blocking | 0 |

**Overall verdict**: Spec 059 is constitutionally sound. Ctags acquisition is safe, definitions-only is not overclaimed, evidence-state honesty is maintained, runtime/parity are not pretended as solved, and the slice genuinely advances the objective. The two minor findings (output-size hygiene and GPL license note) are low-effort mitigations that should be captured before PR merge.
