# PI Review Lane: GLM 5.1

Date: 2026-06-02
Model: `zai/glm-5.1`
Harness: `pi --no-tools --no-context-files --no-session`

## Verdict

assessed:

- Pass with minor conditions.
- The packet is honest about the preflight/topology boundary.
- Runtime topology remains correctly `cannot_verify`.

## Findings

major:

- T009/T010 were incomplete in `tasks.md` despite the packet containing the
  ledger and approval gate.

minor:

- Normalize blocked-command references to include `--docker-compose-plugin`.
- Note that `sha256.txt` size is an aggregate witness, not a self-hash.
- Note that `docker info` contacts the Docker socket even though it is read-only
  by Docker semantics.
- Negative-path preflight evidence is absent; acceptable as future work.

## Disposition

accepted and fixed:

- Marked T009/T010 complete.
- Normalized the canonical blocked create command to
  `./docker-hadoop.sh --docker-compose-plugin --create 1`.
- Added notes about `sha256.txt` and Docker socket read-only semantics.

deferred:

- Controlled negative-path preflight evidence.
