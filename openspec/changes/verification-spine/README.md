# verification-spine

Verification properties — anchors, trust labels, receipts, staleness — are
promoted from internals to the marketed, queryable product spine: one new
`trust.report` tool, a skill mandate for the verification summary, and
positioning whose every claim is anchored to a receipt committed in this
repository.
