# fleet-review-map Tasks

## 1. Data + sheet

- [ ] 1.1 Embed per-province entries in `buildFleetReview` and draw the
      archipelago sheet in `review-template.html` (mini blobs, trust band,
      thin lanes, danger marks, deterministic per-province layout, group
      hull = Chart Room link / honest not-rendered chip)
- [ ] 1.2 Verify tests: embedded entries per province, determinism byte-
      stable, failure paths unchanged; headless: one group per province,
      links correct, zero console errors; screenshot review

## 2. Archive

- [ ] 2.1 Full suite green; archive
