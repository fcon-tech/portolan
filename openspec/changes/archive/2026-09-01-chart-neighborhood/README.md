# chart-neighborhood

Neighborhood query tool over the Chart's fairways: chart.neighborhood(vessel, depth, direction, verify) with fan-in ranking, budget, optional relation typing, shipped with its invocation contract (skill mandate + adoption analytics).
