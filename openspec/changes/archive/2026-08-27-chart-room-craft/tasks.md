# chart-room-craft Tasks

## 1. Rendering

- [x] 1.1 Apply the six fixes in `template.html` (tokens, sea/halo steps,
      tapered lane renderer with casing and fan spread, label ladder,
      port glyph, rhumb/rose placement, legend)
- [x] 1.2 Verify headlessly: geometry checks still green (no overlaps,
      no collisions), lanes are filled paths (not strokes), determinism
      holds; screenshot review against the six gaps

## 2. Archive

- [x] 2.1 Full suite green; archive
