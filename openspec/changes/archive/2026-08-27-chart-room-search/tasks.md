# chart-room-search Tasks

## 1. Controls + logic

- [x] 1.1 Add the find field, trust chips, dangers-only chip, and reset
      pill to `template.html` (dimming via a `filtered` class; find reuses
      `select()`); verify headlessly: substring find selects and centers,
      trust chip dims non-members, dangers-only dims clean vessels, reset
      restores, dimmed vessels stay clickable, both representations

## 2. Verify + archive

- [x] 2.1 Full suite green (unit surface unchanged), regenerate one real
      artifact, archive the change
