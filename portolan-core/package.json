{
  "name": "portolan-core",
  "version": "0.2.0-dev",
  "private": true,
  "description": "Portolan Clean Architecture core: domain, use-cases, ports. Renderer/storage agnostic.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test test/unit/*.test.js && node --test test/bdd-runner.js",
    "test:deps": "node scripts/check-dependency-rule.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ajv": "^8.17.0"
  },
  "license": "MIT"
}
