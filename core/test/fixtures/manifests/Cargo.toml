[package]
name = "harbor-cli"
version = "2.1.0"
edition = "2021"
authors = ["Bigtop <dev@bigtop.apache.org>"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
clap = "4.4"

[dev-dependencies]
pretty_assertions = "1.4"

[build-dependencies]
cc = "1.0"

[features]
default = ["cli"]
cli = []
