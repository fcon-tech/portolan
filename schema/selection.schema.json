{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://portolan.dev/schema/selection.schema.json",
  "title": "Portolan Selection",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version"],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "0.1.0"
    },
    "targets": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/target"
      }
    },
    "metadata": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/input_source"
      }
    },
    "runtime": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/input_source"
      }
    },
    "claims": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/claim_source"
      }
    },
    "black_boxes": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/black_box"
      }
    }
  },
  "$defs": {
    "target": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "kind", "path"],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "kind": {
          "type": "string",
          "enum": ["repository", "service", "package", "runtime", "team", "claim", "unknown"]
        },
        "path": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "input_source": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "path"],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "path": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "black_box": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "kind"],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "kind": {
          "type": "string",
          "enum": ["service", "runtime"]
        },
        "label": {
          "type": "string"
        },
        "metadata": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/input_source"
          }
        },
        "runtime": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/input_source"
          }
        },
        "claims": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/claim_source"
          }
        },
        "expected": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["owner", "dependencies", "runtime-endpoints"]
          }
        }
      }
    },
    "claim_source": {
      "$ref": "#/$defs/input_source"
    }
  }
}
