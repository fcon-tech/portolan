# jscpd Run Evidence

Date: 2026-05-27

## Target

Portolan repository smoke target:
`/home/fall_out_bug/projects/sdp/portolan-039-bounded-jscpd-profile`

This is not Bigtop evidence.

## Command

```bash
jscpd --reporters json \
  --output /tmp/portolan-039-jscpd \
  --max-size 100kb \
  --max-lines 1000 \
  --ignore '**/.git/**,**/.portolan/**,**/node_modules/**,**/vendor/**,**/build/**,**/dist/**,**/target/**,**/generated/**' \
  --noSymlinks \
  --gitignore \
  --silent \
  .
```

## Result

- Exit code: 0.
- Output: `/tmp/portolan-039-jscpd/jscpd-report.json`.
- Output size: 321546 bytes.
- jscpd summary: 63 exact clones, 1269 duplicated lines, 3.52% duplicated
  lines, 594 source files.

## Context Prepare Rerun

The output was copied into `/tmp/portolan-039-context/tool-outputs/` and
`portolan context prepare --force` preserved it.

Observed `tool-registry.json` entry:

```json
{
  "id": "jscpd-jscpd-report-json",
  "family": "jscpd",
  "status": "observed",
  "evidence_state": "metadata-visible",
  "metrics": {
    "duplicate_groups": 63
  }
}
```

Observed `oss-plan.json` state:

```json
{
  "status": "input_present",
  "evidence_state": "metadata-visible",
  "reason": "local jscpd-style output is already present in tool-registry.json"
}
```
