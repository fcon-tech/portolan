# Tasks: Local Binary Bootstrap

- [x] T001 Add `scripts/bootstrap-portolan` with local default output and
  explicit `--out` support.
- [x] T002 Add tests that the bootstrap script help is discoverable and that a
  temporary binary can run `--version`.
- [x] T003 Update root README, agent guide, portable skill, Cursor rule, and
  blind acceptance docs to prefer the local binary bootstrap before `go run`.
- [x] T004 Update backlog and product hypothesis ledgers for
  `GAP-HARNESS-GO` / `GAP-NO-BINARY`.
- [x] T005 Run baseline checks, bootstrap smoke, review disposition, and commit.
